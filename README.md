# Stock Simulator – React + Node + Finnhub

Struktura:
- backend/ – Node + Express, proxy do Finnhub (brak CORS z frontu)
- frontend/ – React (Vite)

## Backend

```bash
cd backend
cp .env.example .env
npm install
npm start
```

## Frontend

```bash
cd frontend
npm install
npm run dev
```
